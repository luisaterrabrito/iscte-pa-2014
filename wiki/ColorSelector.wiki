pa.iscde.packagediagram.colorSelector

= Introduction =

Interface: PackageDiagramColorExtension

Change letter color and background color depending on the package name

= Details =
<code>
public interface PackageDiagramColorExtension {
	
	/**
	 * Change color depending on the package name 
	 * @param element Package of color change
	 * @return color Color of the package chosen 
	 */

        Color changeColorLetter(PackageElement element);
	
       Color changeColorBackground (PackageElement element);
}
</code> 